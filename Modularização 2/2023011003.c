#include <stdio.h>

void compara(int x, int y){
	int i, aux = 0;
	/*maior e menor
	  dividir o menor por 10 até encontrar o numero de algarismos do menor
	  contar quantas vezes foi dividido, para comparar com o maior
	*/
	for(i=0, i<)
}

int main (){
	int x, y;
	
	printf("Digite valores para x e y: ");
	scanf("%d %d", &x, &y);
	
	compara(x, y);
	
	return 0;
}
